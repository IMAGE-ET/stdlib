#!/bin/dash -e
# -*- sh -*-
LSTD1=$1 LSTD2=$2
if [ "$#" -ge 2 ]
then shift 2
fi
if [ ! "$LSTD2" ]
then LSTD2=/dev/null
fi
case $LSTD1 in
awk)
  awk -f /usr/local/share/libstd.awk -f "$LSTD2" "$@"
;;
sh)
  . /usr/local/share/libstd.sh
  . "$LSTD2" "$@"
;;
version)
  echo 1.2.0
esac
